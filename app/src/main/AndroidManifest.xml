<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.inthepocket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/inthepocket2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/inthepocket2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:windowSoftInputMode="adjustResize"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".CongratulationsPage"></activity>
        <activity android:name=".TournamentBracket" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UserInput"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ViewTournaments"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".CreditsPage"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <!--<activity-->
            <!--android:name=".CustomView"-->
            <!--android:label="@string/title_activity_custom_view"-->
            <!--android:theme="@style/AppTheme.NoActionBar"></activity>-->
        <!--<activity-->
            <!--android:name=".tournamentBracket"-->
            <!--android:label="@string/app_name"-->
            <!--android:theme="@style/AppTheme.NoActionBar">-->
        <!--</activity>-->

    </application>

</manifest>